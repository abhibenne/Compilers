#include <stdio.h>
 
int main () {

   int a;
	
   /* for loop execution */
   for( a = 10; a < 20; a = a + 1 ){
      printf("value of a: %d\n", a);
   }
   
   for( a = 10; a < 20; a = a + 1 ){
      printf("value of a: %d\n", a);
      printf("value of a: %d\n", a);
      printf("value of a: %d\n", a);
      while(i<20) {
      	print()
      	}
   }
   
   for(     ; a < 20; a = a + 1 ){
    	print();
    	}
 
 
   for(a=10;   ;a = a + 1){
    	print();
    	}
    	
    
   for(a=10;a < 20;   ){
    	print();
    	}	
    	
 for( a = 10; a < 20; a = a + 1 );
   return 0;
}
